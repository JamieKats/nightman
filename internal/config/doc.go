// Package config loads and validates Nightman's runtime configuration:
// which mocked services to enable, listener ports, per-IP rate limits,
// and the PostgreSQL connection string for the capture store.
package config
